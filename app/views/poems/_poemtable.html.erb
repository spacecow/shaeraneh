<table class="poemgroup">
  <tr>
    <th><%= letter %></th>
  </tr>
  <% group.each do |poem| %>
    <tr>
      <td><%= link_to poem.first_verse, poem %></td>
      <td id="actions">
        <%= link_to t(:edit), edit_poem_path(poem) if can? :edit, poem %> <%= link_to t(:del), poem, :confirm => sure?, :method => :delete if can? :destroy, poem %>
      </td>
    </tr>
  <% end %>
</table>
